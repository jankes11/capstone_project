apiVersion: apps/v1
kind: Deployment
metadata:
  name: capstonefinal1
spec:
  replicas: 2
  progressDeadlineSeconds: 3600
  selector:
    matchLabels:
      app: capstonefinal1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: capstonefinal1
    spec:
      containers:
        - image: jankes11/capstonefinal1
          imagePullPolicy: Always
          name: capstonefinal1
          ports:
            - containerPort: 80

apiVersion: v1
kind: Service
metadata:
  name: capstonefinal1
spec:
  type: LoadBalancer
  selector:
    app: capstonefinal1
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
