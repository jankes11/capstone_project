apiVersion: apps/v1
kind: Deployment
metadata:
  name: finalcapstone
  labels:
    app: finalcapstone
  namespace: default
spec:
  replicas: 2
  progressDeadlineSeconds: 3600
  selector:
    matchLabels:
      app: finalcapstone
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: finalcapstone
    spec:
      containers:
        - image: jankes11/finalcapstone:final
          imagePullPolicy: Always
          name: newimage
          ports:
            - containerPort: 80
---
#Services

apiVersion: v1
kind: Service
metadata:
  name: finalcapstone
  labels:
    app: finalcapstone
spec:
  type: LoadBalancer
  selector:
    app: finalcapstone
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP