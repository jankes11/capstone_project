apiVersion: apps/v1
kind: Deployment
metadata:
  name: capstonefin
  labels:
    app: capstonefin
  namespace: default
spec:
  replicas: 2
  progressDeadlineSeconds: 3600
  selector:
    matchLabels:
      app: capstonefin
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: capstonefin
    spec:
      containers:
        - image: jankes11/capstonefin:final
          imagePullPolicy: Always
          name: newimage
          ports:
            - containerPort: 80
---
#Services

apiVersion: v1
kind: Service
metadata:
  name: capstonefin
  labels:
    app: capstonefin
spec:
  type: LoadBalancer
  selector:
    app: capstonefin
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP