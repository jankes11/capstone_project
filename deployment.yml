apiVersion: apps/v1
kind: Deployment
metadata:
  name: capstonefin1
spec:
  replicas: 2
  progressDeadlineSeconds: 3600
  selector:
    matchLabels:
      app: capstonefin1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: capstonefin1
    spec:
      containers:
        - image: capstonefin1
          imagePullPolicy: Always
          name: capstonefin1
          ports:
            - containerPort: 80

apiVersion: v1
kind: Service
metadata:
  name: capstonefin1
spec:
  type: LoadBalancer
  selector:
    app: capstonefin1
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
